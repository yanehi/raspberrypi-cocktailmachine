#!/bin/bash
echo "$DOCKER_PASSWORD"
echo "$DOCKER_USERNAME"

#echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin

docker version
# docker build -t yannicnevhi/fastapi:$TRAVIS_BRANCH -f docker/common/Dockerfile /api
